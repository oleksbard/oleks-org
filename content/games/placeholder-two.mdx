---
title: Placeholder Two
year: 2025
summary: A second placeholder, just so the grid doesn't look lonely.
tech: [TypeScript, WebGL, Rust]
featured: false
---

A second placeholder so the grid has two cells from day one. Replace with a
real entry by adding `content/games/<slug>.mdx` — no code changes needed.

## Notes

Each entry's frontmatter controls its card on the index (`title`, `year`,
`summary`, `tech`, `cover`, `links`, `featured`) and the metadata on the
detail page. The MDX body is rendered through the shared `<Prose>` wrapper.
